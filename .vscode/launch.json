{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Debug NestServer",
      "request": "launch",
      "command": "yarn run start:dev",
      "cwd": "${workspaceFolder}/server"
    }
  ]
}
